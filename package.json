{
  "name": "effect-mermaid-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.3",
  "license": "MIT",
  "description": "Effect-based Mermaid diagram rendering",
  "scripts": {
    "build": "bun run --filter effect-mermaid build && bun run --filter effect-mermaid-node build && bun run --filter effect-mermaid-react build",
    "test": "bun run --filter effect-mermaid test && bun run --filter effect-mermaid-node test && bun run --filter effect-mermaid-react test",
    "test:ci": "bun run --filter effect-mermaid test:ci && bun run --filter effect-mermaid-node test:ci && bun run --filter effect-mermaid-react test:ci",
    "check": "bun run --filter effect-mermaid check && bun run --filter effect-mermaid-node check && bun run --filter effect-mermaid-react check",
    "dev": "bun run --filter effect-mermaid-web dev",
    "web:build": "bun run --filter effect-mermaid-web build",
    "release": "bunx changeset publish"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.3",
    "@babel/core": "^7.28.5",
    "@babel/plugin-transform-export-namespace-from": "^7.27.1",
    "@babel/plugin-transform-modules-commonjs": "^7.27.1",
    "@changesets/changelog-github": "^0.5.1",
    "@changesets/cli": "^2.29.7",
    "@effect/build-utils": "^0.8.9",
    "@effect/vitest": "^0.27.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^14.3.1",
    "@types/node": "^24.10.1",
    "@vitejs/plugin-react": "^4.7.0",
    "@vitest/coverage-v8": "^3.2.4",
    "autoprefixer": "^10.4.22",
    "babel-plugin-annotate-pure-calls": "^0.5.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@effect-atom/atom": "^0.3.4",
    "@effect-atom/atom-react": "^0.3.4",
    "jsdom": "^27.1.0",
    "mermaid": "^10.9.5",
    "react-colorful": "^5.6.1"
  }
}
